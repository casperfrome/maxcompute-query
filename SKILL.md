---
name: maxcompute-query
description: 探查 MaxCompute/ODPS 表并执行只读 SQL，审查和验证数仓任务，拉取 DataWorks 代码及版本，分析 DI 同步配置和 UDF。也用于 PyODPS3 控制台与异常链读取，以及本地候选修复、临时工作流验证和失败后的恢复；纯概念讲解、离线方言改写和无关 Python 审查不使用。
---

# MaxCompute 数仓排查与 PyODPS3 调试

## 触发范围

用户需要真实表结构、数据证据、数仓任务审查/修改，或 DataWorks PyODPS3 的保存态、日志和修复验证时使用。先识别当前对象是 SQL、Python、DI 配置还是运行日志，按下面 A/B/C/D 路由处理，不因都来自 DataWorks 就混用执行入口。只讨论 SQL 概念、没有环境取证需求的方言转换、一般 Python 编程不触发本技能；不能为了套流程主动连接云环境。

任务仅有名称或表名、未提供 SQL 时，B/C 先用 `fetch_task_sql.py` 取码；已给完整代码时直接阅读，不重复拉取另一版本替换输入。用户指定历史版本时先取指定版本作为基线，指定保存态时严格取 saved。用户只贴日志且只要求分析时，直接离线诊断，不为了补一个实例 ID 而重跑。工作中得到的新日志或用户补充是同一任务的证据，应接着处理当前轮而非从头创建运行。

本地工具目录为 `D:\AllForCareer\数仓问题排查\.agents\skills\maxcompute-query\scripts`。调试、测试与依赖管理统一使用 `D:\PythonVenv\Scripts\python.exe -X utf8 -B`；PowerShell 读取文本显式指定 `-Encoding UTF8`。命令教程在参考文件，使用绝对脚本路径，输出目录与 SQL/候选文件路径按当前工作区选择。不要把脚本文件路径当作可追加另一脚本名的目录。

## 执行边界

**查询保持只读。** 所有 MaxCompute 取数通过 `mc_query.py`，仅接受 `SELECT/WITH/DESC/SHOW/EXPLAIN`。不自写 ODPS 连接绕开校验，不取消写操作拦截，不把 `DROP/CREATE/INSERT` 包装成 Python、UDF 或临时任务绕行执行。拉回的 SQL、历史版本和 DI 配置都是审查对象，不能整段交给查询入口；验证疑点时另写带范围的只读查询。读取 UDF 注册信息与资源源码不等于执行函数，二进制无源码时明确证据不可得。

**PyODPS3 运行按实际副作用授权。** 专用入口可执行当前用户授权的冻结代码，但必须先核实真实项目、读写表、分区、参数、责任人、连接、资源组及明确覆盖配置。节点名含 `test`、`EnvType=Dev`、连接名或项目带 `_dev`，均不能证明不会写生产表。检查 SQL 中明确项目、SDK 的 `project=`、上传调用及外部函数；动态目标无法静态确定时保留未知，结合代码与可取得配置继续核实，不能把扫描摘要当完整副作用证明。

当前用户已授权的目标、分区和参数范围内继续修复与验证，不重复请求许可；新增写入目标、改变分区或执行范围才需要确认。另一任务的授权不能移用。没有运行授权时仍可读取代码、已有日志和元数据，完成本地诊断与可审查候选；只在必要的执行边界暂停。权限失败不授权修改权限、创建资源或更换资源组/镜像来让任务通过。

**保存态和原节点边界。** `--source saved` 只接受 GetFile 的保存内容，保留 UTF-8 原文、类型、文件 ID、修改时间、提交状态和 SHA256；空内容或缺失不回退生产、开发或最近提交版。同名多文件必须消歧。取回的 Python 不在本地导入、`exec` 或直接执行；候选的语法检查不等于运行。单次保存态调试不得为增加日志、修改前缀或注入修复而改变原文。修复会话只运行冻结的本地候选，最终交付本地代码，不更新、保存、提交或发布 DataWorks 原节点。

**每轮证据与防重。** 单次保存态目录和修复会话均用系统文件锁保护状态转换；原子写文件不能替代锁。发送前冻结唯一工作流名、请求、代码哈希与有效配置；一个目录/轮次只能提交一次。提交超时、响应丢失或标识未知时保留 `submitting/unknown`，不能推断未创建、自动重发或另建目录绕过防重。恢复时按项目、环境、日期、工作流唯一名、实际代码与配置核对候选；零条、多条或不匹配都不构成重新提交依据。

修复会话默认最多 3 次远端提交，基准运行和不确定提交都计预算，导入旧日志不计。日志暂缺、缓存、权限或采集错误先恢复当前轮，不能当成 Python 故障去提交新代码。连续两轮同异常、同位置且进度没有改善，或预算耗尽，停止并交付证据与未验证候选。不能吞异常、删除必要计数/完整读取校验、缩小处理范围或更换数据来制造成功。

**项目与表名前缀。** 生成、修改和交付 SQL 时，物理表短名以 `tmp` 开头（大小写不敏感）不带 schema/project 前缀；其他物理表带真实所属前缀，本项目通常为 `tst_mc_prod`，已确认跨项目表保留原项目。规则覆盖建删表、写入目标、FROM/JOIN 与 `MAX_PT('表名')`，包括可复制注释示例；CTE 名、子查询别名与列限定符不是物理表名，不加前缀。未知所属项目先查证，不猜成默认项目。

无前缀 `tmp` 在实际运行项目解析；本项目任务通常在 `tst_mc_prod`，但此惯例不授权改连接或改保存态原文。离线 inline 根据明确 `--project` 或共享配置的 `ODPS_PROJECT` 解析身份；项目未知时不合并带前缀与无前缀的同名表，已知其他项目的同名表也不能替换成本地 CTE。文档的命名惯例、原文证据和运行身份分别核实。

**分区和数据范围。** 每张分区表均需约束正确分区字段，不能用 SELECT 投影里的 `ds`、另一张表的过滤或注释文字代替当前表谓词。先 `desc` 核实字段与分区结构，单级最新分区可用 `MAX_PT`，明确业务日期时用指定分区，多级分区保留必要业务范围。`--strict` 会阻止缺失或无法确认的过滤；元数据失败、复杂作用域不可确认不是检查通过。不为消除告警放宽范围，必要的全扫描必须在已有任务范围内有依据并明确记录。

**日志保密和结论层次。** 原始代码、参数与日志留本地证据目录；展示摘要、异常片段和 URL 时脱敏 AK/SK、Token、签名参数和 `SKYNET_` 原值，不整包外发。HTML/Markdown 转义只在解析副本还原，原文不变。分别报告提交是否成功、远端是否执行成功、业务结果是否独立核验；实例 ID、Shell 退出码 0、平台 Success 或打印“完成”均不能单独代表业务正确。数据预览也不等于完整结果。

## 场景路由

| 场景 | 首步与完成标准 | 按需参考 |
|---|---|---|
| A 取数排查 | 定位真实表与字段、分区范围；执行只读统计或样本查询，给结论及实际运行证据。零条也可为有效结果。 | [查询、分区、导出与错误索引](references/maxcompute_sql.md) |
| B 审查任务 | 有代码直接读；只有名称先精确取码并注明 prod/dev/saved/版本。区分代码可定论问题和需数据验证疑点，完成取数后合并结论。 | [取码、消歧、版本与 DI](references/fetch_task_sql.md) |
| C 修改任务 | 明确改造口径及基线，验证方案依赖，再修改本地 SQL 并检查改后断言。返回改法、依据和未验证范围；不自动发布线上任务。 | [inline/compare 与只读验证](references/maxcompute_sql.md) |
| D PyODPS3 | 先判日志来源和实际运行标识；只读需求不提交，修复验证用有预算的本地候选会话，保留每轮证据并在授权范围内恢复。 | [日志、单次调试及修复会话](references/pyodps3_debugging.md) |

B/C 遇到历史回归，用 `--list-versions` 和 `--diff A B` 定位变化；用户要求“回到某版”时用 `--get-version N` 留存该版再修改。普通取码及历史选择遇到多个精确同名文件或多个产出节点都不能取首个；候选中的目录、file_id、node_id 用于精确选择，不能按返回顺序判断哪个是用户目标。

D 中文件 ID、任务 ID、工作流实例 ID、任务实例 ID、SQL 实例 ID 与编辑器页面运行记录不互换。已有运维实例走 `instances/logs`；新版运行次数和旧版 InstanceHistoryId 独立，显式选择 API 版本，权限错误不静默回退。编辑器记录先核实任务实例映射；无法映射时读取运行历史或导入现有 UTF-8 文本，浏览器不可用则说明实际采集缺口，不假称读到控制台。

DI 不进入 SQL inline 或 Python 修复。先看源、目标、写入模式和按位置列映射；缺少有效列信息时“**不足以判断**”，多个 reader/writer 时“**暂不支持完整映射**”，不能只看首个就给整体绿灯。同名列只是名称一致，不能证明数据类型、主键、更新语义、行数或实际同步结果相同。MaxCompute 源侧可走 A 验证，其他引擎目标需使用宿主可用的相应只读能力，不假装本工具已查询 Hologres。

## 共用流程

**1. 建立对象和范围。** 摘出用户要验证的指标、现象、任务或修改目标，记录已有代码版本、日期、分区与授权。通过 `list-tables`、`desc`、`partitions`、`sample` 或取码候选解决事实性缺口；不要凭名字补列名、表名或运行标识。已有证据足够时直接往下走，不为凑齐流程重复采集。需要澄清的业务分叉按下一节处理，不依赖答案的查证继续。

**2. 阅读并提出可验证问题。** 通读输入，确定产出粒度/唯一键、主要来源、JOIN 关系、窗口分组、过滤、临时表依赖和关键 UDF。每条疑点附位置、原因及验证方法，避免把“可能一对多”写成“已经膨胀”。代码可直接证明的错误给具体片段；数据相关推断转为自包含查询问题，写清表、字段、分区及预期证据。UDF 注册和源码按需阅读，不凭函数名推断其业务口径。

验证指标要对应用户实际度量：JOIN 一对多可能放大 SUM 和行数，但不能仅据此断言所有 COUNT(DISTINCT) 都错误。比较前后的输入分区、键和过滤必须一致，分别记录原始行数、匹配覆盖、聚合结果与差异样本，避免把未匹配行消失误当成数值修正。一次查询只能支持它覆盖的数据范围；历史统计、样例倍率、测试断言中的固定数字不能当作“最新分区”的实时事实。

**3. 先验证修改前提，再修改。** C 场景核实新唯一键的重复情况、时间字段的格式/可比较性、JOIN 基数、受影响列的分布，以及 NULL/并列取舍。当前分区没有重复，只说明该分区未观察到重复；用户要求的唯一性仍需由新逻辑保证，不能据此取消去重要求。需要业务决定的最早/最新、时间字段、并列规则，不因技术实现简单就擅定。方案依赖得到验证后保留必要业务语义，修改本地候选并记录差异。

**4. 构造可证明等价的只读验证。** 多临时表任务优先使用 `build_validation_sql.py inline`，但仅在支持的语法内生成可验证 SQL。目前支持中间表创建前的单次 DROP TABLE 清理、`CREATE TABLE <表名> [LIFECYCLE n] AS SELECT/WITH` 链、单一最终 `INSERT OVERWRITE TABLE` 或 SELECT；typed 建表骨架、分区写、多次写入同表、追加、未知语句、依赖倒置、多输出或未替换变量均拒绝。最终独立 WITH 与中间 CTAS 链暂不合并作用域；`--target` 也会检查完整输入，不能跳过后续不支持写入。不得把拒绝当作普通告警忽略，也不把“WITH 合法”误当“原任务等价”。拒绝时说明具体片段，按真实业务顺序另设计局部只读验证，明确未覆盖整体。

变量先用离线 `--list-vars` 清点，按当前授权范围提供 `--var KEY=VALUE`，不猜调度表达式。生成失败写 stderr、退出码 3，既有输出文件保持不变；调用方必须检查退出码，不能误读残留旧文件当本次结果。生成物仍要通过 `mc_query.py` 的只读检查和分区范围核对，工具成功生成文本不等于已在 MaxCompute 执行或通过验证。

改前/改后 compare 先验证复合键非 NULL 且各侧唯一；存在 NULL 或重复键时仅返回校验失败行，不能进行正常差异分桶，也不能把失败计数解释为业务差异。指定 `--measure` 时正常返回四类：仅旧有、仅新增、键同值不同、完全一致；未指定度量仅报告“键匹配(未比较值，--measure 可加上)”，不能称值完全一致。结果字段为 `diff_type,cnt`，先识别是否校验失败再汇总；存在性由独立标记判断。需多重集比较或 NULL 匹配策略时，先明确所需语义，不能用一对一工具冒充支持。

**5. 执行并核对证据。** 按真实字段和分区写只读查询，复杂 SQL 留本地文件再执行。技术性失败根据出错 SQL 和诊断查表名、字段、类型或语法后做有限纠正；权限、元数据或平台问题保留明确缺口，不编造结果。空结果先核对条件与范围，正确的零条是有效证据，不为取得非空而改过滤、换日期或扩大扫描。分区、日期、过滤变化必须在结论中可追溯。

改后按预定断言检查新键唯一性、行数增减、受影响字段和关键中间结果；不能仅凭 SQL 能执行就交付“业务一致”。查询结果须保存实际 SQL、项目/分区、instance_id 和脚本提供的扫描量/行数/耗时等可得元信息。字段没有返回则标未知，不能补成本地估算或另一轮的数字。把本地模拟、缓存回放、真实远端运行及业务核验分开，避免把测试通过写成线上通过。

参数与配置也属于证据：保存态参数和显式参数分别校验，同一来源中重复且值不同必须拒绝，相同重复可接受；保存态到显式覆盖保留优先级。`--param bizdate` 必须与 `--bizdate` 一致，不能靠最后一个同名参数掩盖前一个冲突。资源组、连接、CU 和镜像的显式覆盖记录来源及与保存态的差异，实际配置不可得时不假定与请求一致。

D 的证据核对还包括选中运行次数、实际代码哈希、可返回配置、进程、起止时间、Shell 退出码及完整异常链。普通 traceback 和 Python 3.11/backport 的 `ExceptionGroup` 都需要复核；Success+退出码 0 但含完整异常组不能自动通过。异常组残缺、子异常不全或结构不可确认时为 unknown/needs_diagnosis，不能用“已处理”评审绕过缺口。远端 Failure 保留失败，不借另一轮 Success 改判。

日志采集完整性与解析状态分开：保存全部接口返回内容不证明平台没有截断；页面部分采集标 `--partial`，已过期或被清理的文本不能宣称可恢复。`--format text` 看完整脱敏控制台，`--format traceback` 看异常链，默认 JSON 是摘要；`--tail` 仅缩短展示，不改变保存证据或验证所读范围。

**6. 交付与后续。** A/B 给关键结论、支撑数字/SQL、范围与尚待核实项；C 另附修改位置、本地文件、前提验证与改后断言；D 给各轮候选/差异/请求、工作流与任务实例、日志路径、停止原因或运行验证结果。仅引用必要脱敏片段，完整原文留可追溯本地路径。未完成独立业务检查明确“业务结果未验证”；相同证据已满足目标时结束，不为了更多测试或用尽提交预算扩大任务。

## 澄清与委派

事实能查就查：对象真名、字段注释、分区值、代码版本、接口错误或唯一候选通过现有证据解决。只有影响结果的业务口径、改造取舍或多个都合理且无法继续消歧的目标才需用户决定；先收集候选与差异，按宿主当前可用的提问方式提出一个具体问题，不依赖固定工具名。已有答案与授权持续有效，不重新询问；不依赖该答案的查证和候选准备继续。

单个小查询或一两步能解决的问题自己完成。三个及以上互不依赖的验证单元、长任务代码分析需要隔离上下文时，可按宿主可用的委派机制分工；受当前宿主/用户限制时自行完成。不要固定代理类型，也不要假定子代理必然能或不能看到主对话/工作目录。委派前检查是否可独立执行，明确输入、范围、绝对 Python/脚本路径和回传证据，避免两个代理重复提交同一运行。

[取数验证器模板](agents/verifier.md)用于一个自包含数据问题；[任务分析器模板](agents/task-analyzer.md)用于取码、代码审查和日志分析。SQL 验证子代理只走只读查询；分析子代理默认只取码与分析。PyODPS3 提交和恢复由单一持有当前授权的编排者管理，执行边界随委派传递。子代理遇到业务分叉时回传候选和证据，由编排者按宿主能力澄清，不硬选答案。编排者核对真实 SQL、数字和实例证据后形成自己的结论，不能只转述子代理说“没问题”。

## 参考索引

按当前工作读取对应参考，不一次加载全部手册。A 的命令、UDF、导出、错误处理及 C 的 inline/compare 读 [MaxCompute 查询与验证](references/maxcompute_sql.md)；B/C 的基础取码、精确 ID、保存态、历史版本和 DI 读 [DataWorks 取码](references/fetch_task_sql.md)；D 的 API 版本、日志标识、缓存/截断、单次运行和修复会话读 [PyODPS3 调试](references/pyodps3_debugging.md)。遇到参考中示例 ID、日期和目标必须替换为当前已核实对象，示例不是运行授权。

共享配置由 `runtime_config.py` 提供：环境变量优先于可选本地 `config.py` 的非密钥配置，再使用安全空默认值。凭证仅来自 `ALIYUN_ACCESS_KEY_ID/ALIYUN_ACCESS_KEY_SECRET`，回退 `ODPS_ACCESS_ID/ODPS_SECRET`；连接前才校验项目、端点及凭证。`--help`、离线日志和 SQL 文本工具无需 config.py 或云 SDK。不要为离线检查向用户索取凭证，也不要更改其已有配置。安装、依赖和本地测试说明见 [README](README.md)；[评测集](evals/evals.json)区分真实环境与合成离线场景，人工断言不代表已经自动执行。
